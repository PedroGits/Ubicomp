before_script:
  - docker version
  - ls
  - sudo apt-get install docker-compose
  - docker-compose version

stages:
  - test

test-stage:
  stage: test
  script:
    - docker build -f Dockerfile_Test .

